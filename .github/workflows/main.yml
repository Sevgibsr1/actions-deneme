name: Merhaba Sevgi Pipeline

on:
  push:
    branches:
      - main
  workflow_dispatch:   # 👈 Bu satır elle çalıştırmayı aktif eder

jobs:
  selamlama:
    runs-on: ubuntu-latest

    steps:
      - name: Kodu Checkout Et
        uses: actions/checkout@v3

      - name: Merhaba Mesajı Yaz
        run: echo "Merhaba Sevgi! GitHub Actions çalışıyor 🚀"

